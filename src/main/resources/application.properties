spring.thymeleaf.cache=false

#======================
#DATA SOURCE
#======================
#Set here configuration for database connection
spring.datasource.url=jdbc:mysql://localhost:3306/bookstoredatabase

#username and secret
spring.datasource.username=root
spring.datasource.password=root

#keep the connection alive if idle for a long time
spring.datasource.dbcp2.test-while-idle= true
spring.datasource.dbcp2.validation-query= SELECT 1

#=======================
# = JPA / HIBERNATE
#=======================

#show or not log for each sql query
spring.jpa.show-sql=true

#with update the schema will be automatically updated accordingly to java entities found in the project
spring.jpa.hibernate.ddl-auto=update

#Allows hibernate to generate sql optimized for a particulare DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect


spring.mail.host=smtp.gmail.com
spring.mail.username=peterwadid1@gmail.com
spring.mail.password=Getd0wnonit
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true